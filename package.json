{
  "name": "fullstack-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npm run start:backend --prefix backend",
    "start:all": "npm run build && npm run start"
  },
  "dependencies": {}
}